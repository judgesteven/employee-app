import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.employeeapp',
  appName: 'Employee Wellness App',
  webDir: 'build'
};

export default config;
